# Copy this file to .env and fill in your key. main.py and app.py load it
# automatically (via python-dotenv), so you never have to export it by hand.
# The key is only used when scoring a new goal for the first time; cached
# goals run with no key and no groq package installed.
GROQ_API_KEY=your_groq_api_key_here
